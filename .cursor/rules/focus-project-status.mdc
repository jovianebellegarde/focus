---
description: Focus project status handoff — current branch, versions, what to do next (update when pausing)
alwaysApply: true
---

# Focus — Project status (memory / handoff)

**Last updated:** 2026-08-08 (late) — **Release 0.5.0 in flight:** Phase 5 (#41) + Phase 6 (#42) on `main`; bumping PyPI `focus-hud` **0.5.0** + Marketplace publish of extension **0.6.0**.  
**Product:** Focus — evidence-only blast-radius HUD. PyPI `focus-hud`, CLI `focus`, IDE `extensions/vscode-focus`. Repo: `jovianebellegarde/focus`.

**Read this first in a new chat.** Update this file when pausing or before switching windows.

---

## Where we are right now

| Item | State |
|---|---|
| **Checked-out branch** | `main` — release 0.5.0 |
| **Open PRs** | **None** |
| **PyPI** | Bumping to **`focus-hud` 0.5.0** (tag `v0.5.0` → publish.yml → PyPI + GitHub Release) |
| **Extension** | **0.6.0** — Marketplace publish (was held) |
| **Phase 5 / 6** | **Merged** on main (#41 / #42) |
| **Agent teaching** | `upskilling.mdc`; Mermaid colors = `mermaid-role-colors.mdc` |

### Do next (after release settles)

1. Confirm PyPI `0.5.0` + GitHub Releases Latest
2. Confirm Marketplace listing for `jovianebellegarde.focus-hud`
3. Parked: dynamic-import Explore; per-file headers; check-run annotations

### Standing (agent + owner)

- Feature branch + PR unless `ALLOW_MAIN_COMMIT` (releases may use main)
- Dogfood before commit/push; own PR Test plans; README honesty
- Git author: `71949760+jovianebellegarde@users.noreply.github.com` (or `71949760@users.noreply.github.com`)
- GitHub / Marketplace / PyPI Trusted Publisher owner: **`jovianebellegarde`** (not `j0viane`)
