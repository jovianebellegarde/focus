---
description: Focus project status handoff — current branch, versions, what to do next (update when pausing)
alwaysApply: true
---

# Focus — Project status (memory / handoff)

**Last updated:** 2026-08-08 (evening) — **housekeeping done**: `docs/PUBLISH.md` now documents tag → PyPI **and** GitHub Release (#40); leftover `ci/auto-github-release` branch deleted (local + remote); handoff refreshed. Still: **`focus-hud` 0.4.0 live on PyPI + GitHub Releases (Latest)**; Phase **4b core complete**; extension **0.6.0** (Marketplace held); `main` @ **`0beb46d`** (+ this housekeeping commit)  
**Product:** Focus — evidence-only blast-radius HUD. PyPI `focus-hud`, CLI `focus`, IDE `extensions/vscode-focus`. Repo: `j0viane/focus`.

**Read this first in a new chat.** Update this file when pausing or before switching windows.

---

## Where we are right now

| Item | State |
|---|---|
| **Checked-out branch** | `main` @ **`0beb46d`** — **up to date** with `origin/main`. `200 passed` (pre-#40 release suite; #40 is CI-only). Local dirty: this handoff file only |
| **Open PRs** | **None** |
| **`origin/main` HEAD** | **`0beb46d`** — *Merge PR #40* (auto GitHub Release on tag). Prior: release `d7ae6bf` (0.4.0) ← #39 ← #38 ← #37 ← #36 |
| **PyPI** | **`focus-hud` 0.4.0** — live (`pip install "focus-hud>=0.4.0"`). Tag `v0.4.0`, publish CI green. Includes #32/#33/#34/#37/#38. `.venv` synced (`focus version` → 0.4.0) |
| **GitHub Releases** | **`focus-hud 0.4.0` = Latest** ([releases/tag/v0.4.0](https://github.com/j0viane/focus/releases/tag/v0.4.0)). `v0.3.5` backfilled. Older: 0.3.1 → 0.1.0 |
| **Extension** | **0.6.0** — why-this-edge import jump (#37) + JSDoc/TSDoc captions (#38). **Not** on Marketplace (held); sideload `./scripts/install-extension.sh` |
| **LLM captions** | Opt-in; default **`qwen2.5-coder:3b`**; `MAX_LABEL_CHARS = 320`; prompt rev **`320-ground-v3`** (#34); never on overlay |
| **Phase 4b** | **Core complete** — symbol-proven downstream (#33), why-this-edge (#37), JSDoc/TSDoc (#38), ROA hard caps + guard tests (#39). Dynamic-import / string-literal hints **parked to Explore** (topology gate) |
| **Phase 4c / 4d** | Caption labeler + ledger on PyPI 0.4.0 (#29/#30/#32/#34) |
| **Publish CI** | Tag `v*` → PyPI **and** (after #40) `gh release create` with `--generate-notes`. `permissions: contents: write` |
| **Cache** | Local pack-hash memory+disk only — **no Redis** |
| **Marketplace** | **Held** until owner says go |
| **Agent teaching** | Canonical **only** private `cursor-rules` → `rules/upskilling.mdc` + hooks. Recap in **same reply** (no stop-hook). Eng §17 = owner-owned co-write |
| **Agent gauntlet** | `agent-verification-gauntlet.mdc` — **no change needed** (tonight’s miss was release-surface: tag≠GitHub Release; fixed in #40 + Releases backfill). Dogfood stays Focus gates §9 |

### Merged this session (2026-08-06 / 07 UTC)

| PR | What |
|---|---|
| **#36** | Roadmap honesty header |
| **#37** | Why-this-edge import jump (+ seed-file CodeLens fix `148a761`) |
| **#38** | JSDoc/TSDoc → `Definition.docstring` |
| **#39** | Close Phase 4b core — ROA caps marked ✅ + caps guard tests |
| **release** | `d7ae6bf` — `focus-hud` 0.4.0 / extension 0.6.0 → PyPI |
| **#40** | `publish.yml` auto-creates GitHub Release on tag push |

**Branches / worktrees:** all prior feature worktrees deleted. `ci/auto-github-release` deleted **local + remote** (2026-08-08). Only `main` remains.

**Chosen approaches (locked):** #32 class-body + Typer; #33 filter file-level rings (not rebuild symbol graph); #34 prompt/pack on 3b first; JSDoc shipped #38; ROA caps keep 260/110/110/2 + ℹ️ clip 52.

### Locked UX (do not regress without options table)

1. Captions: sweet-spot what/why; up to two short sentences; ≤320 chars; `prefer_polish_label`
2. Body edit wins over docstring for ℹ️ anchor (`_run_is_docstring_only`)
3. Drop IDE risk rail on `def` — implication stays in HUD/JSON/PR only
4. With `focus.llmCaptions`: wait for open-file LLM before first paint (no short deterministic flash); rest background
5. Never LLM on live overlay / save path
6. One `explanationLensTitle` CodeLens — Cursor joins same-line lenses with ` | `; no multi-lens wrap
7. Tiny + low blast → pass-through; path / Danger Zone / shared-layout seeds still force full HUD even on a one-line edit
8. Seed file still gets top “who imports me” why-edge CodeLens alongside per-symbol ℹ️ (#37 fix)

### Smoke / dogfood recipes

**#32 ledger:** Edit a class-body constant with a method reader (stranger name e.g. `RETRY_LIMIT`). Audit Local **without** LLM. Pass if ℹ️ names scope (`read by …`), not orphan fallback.

**#33 symbol filter:** glass_box — edit only `hash_password` → 0 downstream; edit only `validate_token` → 4 downstream + Mermaid.  
`.venv/bin/focus audit --local --path <fixture> --base main`

**#34 captions:** Clear cache after rev bump; `focus audit --local --llm-captions` with Ollama 3b.

**#37 why-edge:** In IDE, CodeLens on hop-1 / seed “who imports me” jumps to proving import line.

**ROA caps:** Long return/assign ℹ️ clips with `…` (slot ≤52); explanation ≤260.

Other notes:

- `.venv/bin/focus` == PyPI 0.4.0. Prefer either for dogfood.
- Workspace `.vscode/settings.json` may set `focus.llmCaptions: true` (gitignored)
- Tiny-diff fixture idea: `/tmp/focus-tiny-dogfood` / glass_box billing pattern
- **cursor-rules hook (2026-08-06):** `ask-before-git-publish.sh` — (a) gates `git -c … commit` (`7b76703`); (b) tag / feature-branch pushes from a `main` checkout are **not** treated as main publishes (`5a5fb64`). Known Cursor gap: `beforeShellExecution` may not fire for agent tool-shell after a **mid-session restart** — owner-ask stays the real backstop
- **Tag ≠ Release lesson:** pushing `vX.Y.Z` alone used to update PyPI but not the GitHub Releases page. #40 ties them. Still confirm Releases page after the next tag
- `docs/PUBLISH.md` now documents tag → PyPI **and** GitHub Release (#40) — honesty follow-up ✅ done 2026-08-08

### Do next

1. **Phase 5** — GitHub inline diff annotations (reuse `--format json`) — **next real bet** (options table before code: LLM/topology gate n/a, but per-file comment vs check-run annotation is a design choice)
2. Optional: **Marketplace** publish of extension 0.6.0 — **held** until owner says go
3. Dynamic-import / string-literal hints — **parked to Explore** (topology gate; options table before code)
4. Caption quality follow-up from #34 *(owner)* / optional `qwen2.5-coder:7b` bake-off
5. Optional: vulture/radon on `explain.py` only (~2.5k LOC; not a rewrite)
6. Verify publish hook re-arms in a **fresh** Cursor session

*(Housekeeping 2026-08-08 done: PUBLISH.md honesty, ci branch delete, handoff refresh — committed to `main` w/ ALLOW_MAIN_COMMIT.)*

### Standing (agent + owner)

- No commit/push/PR unless asked — **ask explicitly**; never infer from dogfood “works”/“yup”
- **Feature branch + PR** for Focus — never ship straight to `main` unless explicit `ALLOW_MAIN_COMMIT` (`focus-agent-gates.mdc` §10). Private **`cursor-rules` may use `main`**
- **Dogfood before commit/push** — real CLI/IDE on a concrete edit; show evidence; wait for owner OK (§9). Pytest ≠ product proof
- Own PR Test plan: automatable boxes `[x]` with evidence; leave only true *(owner)* items
- README honesty on user-visible changes; honest about PyPI vs this checkout; **also check GitHub Releases Latest** after a version tag
- Dogfood CLI: `.venv/bin/focus` (0.4.0) or published `focus-hud>=0.4.0`
- Captions must stay portable — no Focus-lore special cases
- LLM never on live overlay; packs only; never full files / full graph
- Teaching = **`upskilling.mdc` only** — expand acronyms; no-CS-school bar; recap what/why/connects/breaks **in the same reply**; **no interview one-liners** unless she asks; **no stop-hook second turn**
- Git author for push: GitHub noreply `71949760+j0viane@users.noreply.github.com` (avoids GH007)
