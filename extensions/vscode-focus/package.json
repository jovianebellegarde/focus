{
  "name": "focus-hud",
  "displayName": "Focus",
  "description": "Blast radius you can defend - evidence-only Focus HUD in the editor.",
  "version": "0.6.0",
  "publisher": "jovianebellegarde",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jovianebellegarde/focus.git"
  },
  "homepage": "https://github.com/jovianebellegarde/focus#readme",
  "bugs": {
    "url": "https://github.com/jovianebellegarde/focus/issues"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Other", "SCM Providers"],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "focus.auditLocal",
        "title": "Focus: Audit Local Changes"
      },
      {
        "command": "focus.traceCurrentFile",
        "title": "Focus: Trace Current File"
      },
      {
        "command": "focus.showHud",
        "title": "Focus: Show HUD"
      },
      {
        "command": "focus.showWhy",
        "title": "Focus: Show Why (blast radius reason)"
      },
      {
        "command": "focus.whyEdge",
        "title": "Focus: Why This Edge (jump to proving import)"
      },
      {
        "command": "focus.refresh",
        "title": "Focus: Refresh"
      },
      {
        "command": "focus.showEvidence",
        "title": "Focus: Show Evidence (trust cues)"
      }
    ],
    "configurationDefaults": {
      "editor.codeLens": true,
      "diffEditor.codeLens": true
    },
    "configuration": {
      "title": "Focus",
      "properties": {
        "focus.path": {
          "type": "string",
          "default": "",
          "description": "Path to the focus executable. Empty = search PATH."
        },
        "focus.base": {
          "type": "string",
          "default": "main",
          "description": "Git base ref for focus audit --local."
        },
        "focus.gutter": {
          "type": "boolean",
          "default": true,
          "description": "Show gutter highlights on changed symbols and blast-radius files."
        },
        "focus.captionHighlight": {
          "type": "boolean",
          "default": true,
          "description": "Deprecated - use focus.inlineExplanations. Kept for compatibility."
        },
        "focus.inlineExplanations": {
          "type": "boolean",
          "default": true,
          "description": "Inline detail at the primary edit (virtual - not saved to disk)."
        },
        "focus.lensFontSize": {
          "type": "number",
          "default": 0,
          "description": "CodeLens font size in px for Focus rows. 0 = editor default (~90% of editor font). -1 = match editor.fontSize. Otherwise sets editor.codeLensFontSize in this workspace."
        },
        "focus.autoAuditOnSave": {
          "type": "boolean",
          "default": true,
          "description": "After you save a source file, quietly re-run Focus audit and refresh CodeLens / gutters in place (no HUD pop)."
        },
        "focus.liveBufferOverlay": {
          "type": "boolean",
          "default": true,
          "description": "While you edit (even before Save), quietly re-audit using the unsaved buffer so CodeLens tracks what you see."
        },
        "focus.llmCaptions": {
          "type": "boolean",
          "default": false,
          "description": "Opt-in: on Focus: Audit Local, wait for LLM captions on the open file before painting (no short deterministic flash), then label the rest in the background. Never on autosave / overlay. Needs FOCUS_LLM_API_KEY for openai/anthropic, or FOCUS_LLM_PROVIDER=ollama (default model qwen2.5-coder:3b)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.3"
  }
}
