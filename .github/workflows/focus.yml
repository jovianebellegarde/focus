# Opt-in Focus HUD on pull requests.
# Permissions: contents:read (checkout) + pull-requests:write (comment only).
# See docs/PRIVACY.md.

name: Focus

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  hud:
    name: Focus HUD
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@v8.3.2
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.12

      - name: Install Focus
        run: uv sync --frozen

      - name: Run Focus audit
        env:
          FOCUS_BASE: ${{ github.event.pull_request.base.sha }}
        run: |
          uv run focus audit --base "$FOCUS_BASE" --out focus-hud.md

      - name: Post or update PR comment
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FOCUS_PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          uv run python -c "from pathlib import Path; from focus.ci import post_from_env; print(post_from_env(Path('focus-hud.md')))"

      # Surface C — inline ℹ️ captions on changed lines (Files changed tab).
      # Same pull-requests:write scope; blast radius stays in the PR comment above.
      - name: Focus audit (JSON for inline)
        env:
          FOCUS_BASE: ${{ github.event.pull_request.base.sha }}
        run: |
          uv run focus audit --base "$FOCUS_BASE" --format json --out focus-hud.json

      - name: Post inline review comments
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FOCUS_PR_NUMBER: ${{ github.event.pull_request.number }}
          FOCUS_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          uv run python -c "from pathlib import Path; from focus.ci import post_review_from_env; print(post_review_from_env(Path('focus-hud.json')))"
