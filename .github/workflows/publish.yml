# Publish focus-hud to PyPI on version tags (v0.1.0, …) via Trusted Publishing.
# One-time setup: https://docs.pypi.org/trusted-publishers/
#   PyPI project: focus-hud
#   Workflow: publish.yml on jovianebellegarde/focus, environment: pypi

name: Publish

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    environment: pypi
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v8.3.2

      - name: Build
        run: uv build

      - name: Publish
        uses: pypa/gh-action-pypi-publish@release/v1

      # A tag push publishes to PyPI but does NOT create a GitHub Release —
      # that is a separate object. Create one so the Releases page tracks tags.
      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            --title "focus-hud ${GITHUB_REF_NAME#v}" \
            --generate-notes \
            --verify-tag
